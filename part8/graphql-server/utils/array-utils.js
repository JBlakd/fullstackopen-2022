// const uniqueItems = (list, key) => {
//     const reduced = list.reduce((objSet, curObj) => objSet.add(list[key]), new Set)
//     console.log('reduced', reduced)
//     return reduced.size
// }

// module.exports = {
//     uniqueItems
// }